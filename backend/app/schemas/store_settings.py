"""
TODO (feature/customization): Pydantic schemas for StoreSettings, matching
IMPLEMENTATION_PLAN.md sections 6.6 and 14.

- StoreSettingsPublic: only fields the storefront needs (name, description,
  logo_url, banner_url, primary_color, secondary_color, contact info)
- StoreSettingsUpdate: same fields, used by PUT /api/v1/admin/store/settings
"""
