# API Reference

Interactive docs are always available while the backend is running:

```text
http://localhost:8000/docs
```

TODO: as endpoints move from stub to implemented, keep
`IMPLEMENTATION_PLAN.md` sections 7-14 as the spec and use this file for
anything the auto-generated Swagger docs don't cover (e.g. auth flow notes,
error response shape).
