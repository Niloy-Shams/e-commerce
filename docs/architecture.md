# Architecture

TODO: fill in as the system takes shape. At minimum, before the final
submission this should cover:

- High-level diagram: Next.js (frontend) <-> FastAPI (backend) <-> PostgreSQL.
- Why this stack was chosen.
- Request flow for one representative feature (e.g. checkout).

See `IMPLEMENTATION_PLAN.md` for the authoritative spec this project follows.
