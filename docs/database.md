# Database Schema

Source of truth for now: `IMPLEMENTATION_PLAN.md`, section 6 (User, Product,
Order, OrderItem, StoreSettings) and the SQLAlchemy models in
`backend/app/models/`.

TODO: once migrations exist, add an ER diagram here and keep it in sync
whenever a migration changes the schema.
