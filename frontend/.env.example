# Copy this file to .env.local for local development.
# Docker Compose loads this automatically via env_file.
# Use the Docker service name so the frontend container can reach the backend.
# For local development outside Docker, override with http://localhost:8000/api/v1.
NEXT_PUBLIC_API_URL=http://backend:8000/api/v1
