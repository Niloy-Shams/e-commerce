# Copy this file to .env in the project root.
# docker-compose.yml reads this automatically for ${VARS} substitution.
# Backend and frontend still keep their own .env files
# (backend/.env, frontend/.env.local) for app-level config.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=ecommerce
